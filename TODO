*figure out the 2 buttons activate thing
*normal key listener in the window itself
*set up macros      // the app has to hide itself first then paste whatever
//

*create multiple frames 
frames:- Java , python , miscs
